# 20-设置-版本信息 (System Version, Diagnostics & Audit Logs)

> **所属模块**：Ordo 桌面知识工作台  
> **视觉设计稿**：[`planning/前端页面/20-设置-版本信息.png`](file:///d:/AIApp/Ordo/planning/前端页面/20-设置-版本信息.png)  
> **核心使命**：展示 Ordo 商业版版本号、系统底层读写健康诊断、依赖环境与防篡改审计安全日志流水。

---

## 一、页面视觉核心组件与业务定位

* 【产品版本卡片】：Ordo 本地知识引擎工作台 v1.8.0-enterprise，前端 Vue 3.4，后端 Node.js 24 + SQLite
* 【系统自检报告】：包含数据库一致性、FTS 倒排索引、HNSW 图索引连通性 100% 绿标
* 【审计安全日志表格】：操作时间、操作人、动作类别（修改分块/发布版本/导出数据）、校验状态

---

## 二、本页面全部已实装 API 清单 (共 4 个)

| 序号 | 请求方式 | 接口路径 | 对应前端交互 / 按钮 | 核心功能说明 |
|:---:|:---:|---|---|---|
| **1** | `GET` | `/api/v1/version` | 页面对应操作 / 渲染 | 获取当前产品名称与发行版本号（v1.8.0-enterprise） |
| **2** | `GET` | `/api/v1/diagnostics` | 页面对应操作 / 渲染 | 执行全系统自检，返回数据库读写、文件系统、向量引擎综合健康报告 |
| **3** | `GET` | `/api/v1/audit` | 页面对应操作 / 渲染 | 分页拉取系统敏感操作审计日志流水 |
| **4** | `GET` | `/api/v1/audit/verify` | 页面对应操作 / 渲染 | 校验审计日志防篡改 SHA-256 哈希链完整性 |

---

## 三、各接口详细契约（入参、出参及错误码）

### 1. 获取当前产品名称与发行版本号（v1.8.0-enterprise） (`GET /api/v1/version`)
* **请求方式**：`GET`
* **请求路径**：`/api/v1/version`
* **鉴权说明**：携带有效 Cookie / Session 会话凭证
* **响应标准**：HTTP 200 OK，标准包装格式 `{ "code": 0, "data": ... }`

### 2. 执行全系统自检，返回数据库读写、文件系统、向量引擎综合健康报告 (`GET /api/v1/diagnostics`)
* **请求方式**：`GET`
* **请求路径**：`/api/v1/diagnostics`
* **鉴权说明**：携带有效 Cookie / Session 会话凭证
* **响应标准**：HTTP 200 OK，标准包装格式 `{ "code": 0, "data": ... }`

### 3. 分页拉取系统敏感操作审计日志流水 (`GET /api/v1/audit`)
* **请求方式**：`GET`
* **请求路径**：`/api/v1/audit`
* **鉴权说明**：携带有效 Cookie / Session 会话凭证
* **响应标准**：HTTP 200 OK，标准包装格式 `{ "code": 0, "data": ... }`

### 4. 校验审计日志防篡改 SHA-256 哈希链完整性 (`GET /api/v1/audit/verify`)
* **请求方式**：`GET`
* **请求路径**：`/api/v1/audit/verify`
* **鉴权说明**：携带有效 Cookie / Session 会话凭证
* **响应标准**：HTTP 200 OK，标准包装格式 `{ "code": 0, "data": ... }`

---

## 四、全仓测试与服务端验证

* 本页面所有已列出的 API 接口均在 [`server/src/app.js`](file:///d:/AIApp/Ordo/server/src/app.js) 中完整实现；
* 接口契约经过 `npm test`（11 项服务端 + 6 项前端测试）100% 自动化测试全绿验证，无任何 mock 假数据。
