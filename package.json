{
  "name": "ordo",
  "version": "1.0.0",
  "private": true,
  "description": "Ordo local-first knowledge product",
  "scripts": {
    "start": "npm --prefix server start",
    "seed": "npm --prefix server run seed",
    "test": "npm --prefix server test && npm --prefix web test",
    "check": "npm --prefix server run check && node --check web/app.js"
  }
}
