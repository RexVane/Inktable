{
  "name": "ordo",
  "version": "1.0.0",
  "private": true,
  "description": "Ordo local-first knowledge product",
  "scripts": {
    "start": "python ordo.py serve",
    "seed": "python ordo.py seed",
    "test": "python ordo.py test -q && npm --prefix web test",
    "check": "python ordo.py check && node --check web/api.js && node --check web/app.js"
  }
}
